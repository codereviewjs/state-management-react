{
  "name": "state-management-react",
  "private": true,
  "workspaces": [
    "administrator-app",
    "backend",
    "ui",
    "types",
    "news-site",
    "packages/*"
  ],
  "version": "1.0.0",
  "main": "index.js",
  "author": "codereviewjs <codereviewjs@gmail.com>",
  "license": "MIT",
  "scripts": {
    "init:project": "yarn seed & yarn build:types & yarn build:ui",
    "seed": "yarn workspace backend seed",
    "BUILD SCRIPTS": "------------------------",
    "build:types": "yarn workspace types build",
    "build:ui": "yarn workspace ui build",
    "START SCRIPTS": "------------------------",
    "start:ui": "yarn workspace ui storybook",
    "start:backend": "yarn workspace backend start",
    "start:news": "yarn workspace news-site dev",
    "start:administrator": "yarn workspace administrator-app dev"
  }
}
